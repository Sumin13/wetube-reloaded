extend base.pug
include mixins/video

block content 
 form(method="GET")
  input(placeeholder="Search by title", name="keyword", type="text")
  input(type="submit", value="search now")

 div
  each video in videos 
   +video(video)